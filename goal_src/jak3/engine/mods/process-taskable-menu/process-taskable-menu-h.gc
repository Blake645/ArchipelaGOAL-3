;;-*-Lisp-*-
(in-package goal)

#| This file defines a process-taskable menu structure and its related lists.
   Each menu list corresponds to a specific cutscene, game-task-node, actor, etc,
   based on mission/task progress.

   Also, check the method `setup-fields` of the `warp-gate` type in `warp-gate-menu-data.gc`, 
   which is responsible for assigning a menu entry list to a specific entity. Do this if you
   want to add custom warp-gate menus in your own custom levels.  
 |#

(deftype taskable-menu-entry (structure)
  ((task-node     game-task-node)    ;; the game-task-node containing the introductory data for a mission (cutscenes, actors, etc).
   (event-idx     int)               ;; the index under `when-open` related to this menu item.
   (continue      string)            ;; the name of the continue point needed to load the cutscene from the event object.
   (text-id       text-id)           ;; the text-id that defines the phrase to be displayed for this menu item.
   (exclusive?    symbol)            ;; optional: if true and item is available, it will be the only menu item in the list (first come first serve).
   (allow-when    (function symbol)) ;; optional: function that must return true for this entry to become available
   (forbid-when   (function symbol)) ;; optional: function that hides this entry if it returns true, regardless of `allow-when`
   )  
  )

(defmacro static-taskable-menu-entry 
  (&key task-node
    &key event-idx
    &key continue
    &key text-id
    &key (exclusive? #f)
    &key (allow-when (lambda () #f))
    &key (forbid-when (lambda () #f))
    )
  `(new 'static 'taskable-menu-entry
     :task-node ,task-node
     :event-idx ,event-idx
     :continue ,continue
     :text-id ,text-id
     :exclusive? ,exclusive?
     :allow-when ,allow-when
     :forbid-when ,forbid-when
     )
  )

;; just an empty array. leave it at is.
;; used as a fallback when an entity doesn't have a defined menu.
(define *empty-taskable-menu-list* (new 'static 'boxed-array :type taskable-menu-entry))

(define *kleever-menu-list*
  (new 'static 'boxed-array :type taskable-menu-entry
    (static-taskable-menu-entry
      :task-node (game-task-node wascity-chase-introduction)
      :event-idx 0
      :continue "wasstada-entrance"
      :text-id (text-id progress-missions-wascity-chase)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node wascity-chase-introduction))
            (task-node-open? (game-task-node wascity-chase-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node wascity-chase-resolution)))
      )
    (static-taskable-menu-entry
      :task-node (game-task-node desert-turtle-training-introduction)
      :event-idx 0
      :continue "wasdoors-city"
      :text-id (text-id progress-missions-desert-turtle-training)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node desert-turtle-training-introduction))
            (task-node-open? (game-task-node desert-turtle-training-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node desert-turtle-training-resolution)))
      )
    (static-taskable-menu-entry
      :task-node (game-task-node desert-hover-introduction)
      :event-idx 0
      :continue "wasdoors-city"
      :text-id (text-id progress-missions-desert-hover)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node desert-hover-introduction))
            (task-node-open? (game-task-node desert-hover-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node desert-hover-resolution)))
      )
    (static-taskable-menu-entry
      :task-node (game-task-node desert-catch-lizards-introduction)
      :event-idx 0
      :continue "wasdoors-city"
      :text-id (text-id progress-missions-desert-catch-lizards)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node desert-catch-lizards-introduction))
            (task-node-open? (game-task-node desert-catch-lizards-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node desert-catch-lizards-resolution)))
      )
    (static-taskable-menu-entry
      :task-node (game-task-node wascity-gungame-introduction)
      :event-idx 0
      :continue "wascityb-gungame"
      :text-id (text-id progress-missions-wascity-gungame)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node wascity-gungame-introduction))
            (task-node-open? (game-task-node wascity-gungame-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node wascity-gungame-resolution)))
      )
    (static-taskable-menu-entry
      :task-node (game-task-node desert-beast-battle-introduction)
      :event-idx 0
      :continue #f
      :text-id (text-id progress-missions-desert-beast-battle)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node desert-beast-battle-introduction))
            (task-node-open? (game-task-node desert-beast-battle-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node desert-beast-battle-resolution)))
      )
  )
)
(define *damus-menu-list*
  (new 'static 'boxed-array :type taskable-menu-entry
    (static-taskable-menu-entry
      :task-node (game-task-node desert-artifact-race-1-introduction)
      :event-idx 0
      :continue "wasdoors-city"
      :text-id (text-id progress-missions-desert-artiface-race-1)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node desert-artifact-race-1-introduction))
            (task-node-open? (game-task-node desert-artifact-race-1-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node desert-artifact-race-1-resolution)))
       )
    (static-taskable-menu-entry
      :task-node (game-task-node arena-fight-2-introduction)
      :event-idx 0
      :continue "waspala-gun-training"
      :text-id (text-id progress-missions-arena-fight-2)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node arena-fight-2-introduction))
            (task-node-open? (game-task-node arena-fight-2-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node arena-fight-2-resolution)))
       )
    (static-taskable-menu-entry
      :task-node (game-task-node desert-rescue-introduction)
      :event-idx 0
      :continue "waspala-start"
      :text-id (text-id progress-missions-desert-rescue)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node desert-rescue-introduction))
            (task-node-open? (game-task-node desert-rescue-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node desert-rescue-resolution)))
       )
    (static-taskable-menu-entry
      :task-node (game-task-node arena-fight-3-introduction)
      :event-idx 0
      :continue "wasstada-pre-fight-1"
      :text-id (text-id progress-missions-arena-fight-3)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node arena-fight-3-introduction))
            (task-node-open? (game-task-node arena-fight-3-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node arena-fight-3-resolution)))
       )
    (static-taskable-menu-entry
      :task-node (game-task-node nest-eggs-introduction)
      :event-idx 0
      :continue "waspala-nest"
      :text-id (text-id progress-missions-nest-eggs)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node nest-eggs-introduction))
            (task-node-open? (game-task-node nest-eggs-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node nest-eggs-resolution)))
       )
    (static-taskable-menu-entry
      :task-node (game-task-node desert-jump-mission-introduction)
      :event-idx 0
      :continue "waspala-start"
      :text-id (text-id progress-missions-desert-jump-mission)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node desert-jump-mission-introduction))
            (task-node-open? (game-task-node desert-jump-mission-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node desert-jump-mission-resolution)))
       )
    )
  )
(define *seem-menu-list*
  (new 'static 'boxed-array :type taskable-menu-entry
    (static-taskable-menu-entry
      :task-node (game-task-node wascity-pre-game-introduction)
      :event-idx 0
      :continue "wascityb-game"
      :text-id (text-id progress-missions-wascityb-game)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node wascity-pre-game-introduction))
            (task-node-open? (game-task-node wascity-pre-game-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node wascity-pre-game-resolution)))
       )
    (static-taskable-menu-entry
      :task-node (game-task-node wascity-leaper-race-introduction)
      :event-idx 0
      :continue #f
      :text-id (text-id progress-missions-wascity-leaper-race)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node wascity-leaper-race-introduction))
            (task-node-open? (game-task-node wascity-leaper-race-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node wascity-leaper-race-resolution)))
       )
  )
)
(define *ashelin-menu-list*
  (new 'static 'boxed-array :type taskable-menu-entry
    (static-taskable-menu-entry
      :task-node (game-task-node desert-oasis-defense-introduction)
      :event-idx 0
      :continue "desert-ashelin"
      :text-id (text-id progress-missions-desert-oasis-defense)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node desert-oasis-defense-introduction))
            (task-node-open? (game-task-node desert-oasis-defense-meeting))
            (task-node-open? (game-task-node desert-oasis-defense-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node desert-oasis-defense-resolution)))
       )
    (static-taskable-menu-entry
      :task-node (game-task-node city-protect-hq-introduction)
      :event-idx 0
      :continue "ctyslumc-freehq-exit"
      :text-id (text-id progress-missions-city-protect-hq)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node city-protect-hq-introduction))
            (task-node-open? (game-task-node city-protect-hq-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node city-protect-hq-resolution)))
       )
    (static-taskable-menu-entry
      :task-node (game-task-node sewer-hum-kg-introduction)
      :event-idx 0
      :continue #f
      :text-id (text-id progress-missions-sewer-hum-kg)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node sewer-hum-kg-introduction))
            (task-node-open? (game-task-node sewer-hum-kg-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node sewer-hum-kg-resolution)))
       )
    (static-taskable-menu-entry
      :task-node (game-task-node city-blow-tower-introduction)
      :event-idx 0
      :continue "cty-blow-tower-mission"
      :text-id (text-id progress-missions-city-blow-tower)
      :allow-when (lambda ()
        (or (task-node-open? (game-task-node city-blow-tower-introduction))
            (task-node-open? (game-task-node city-blow-tower-resolution))
            )
        )
      :forbid-when (lambda () (task-node-closed? (game-task-node city-blow-tower-resolution)))
       )
    )
  )
  (define *torn-menu-list*
    (new 'static 'boxed-array :type taskable-menu-entry
      (static-taskable-menu-entry
        :task-node (game-task-node city-port-fight-introduction)
        :event-idx 0
        :continue "hiphog-start"
        :text-id (text-id progress-missions-city-port-fight)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node city-port-fight-introduction))
              (task-node-open? (game-task-node city-port-fight-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node city-port-fight-resolution)))
         )
      (static-taskable-menu-entry
        :task-node (game-task-node city-port-attack-introduction)
        :event-idx 0
        :continue "hiphog-start"
        :text-id (text-id progress-missions-city-port-attack)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node city-port-attack-introduction))
              (task-node-open? (game-task-node city-port-attack-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node city-port-attack-resolution)))
         )
      (static-taskable-menu-entry
        :task-node (game-task-node city-sniper-fight-introduction)
        :event-idx 0
        :continue "hiphog-start"
        :text-id (text-id progress-missions-city-sniper-fight)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node city-sniper-fight-introduction))
              (task-node-open? (game-task-node city-sniper-fight-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node city-sniper-fight-resolution)))
         )
      (static-taskable-menu-entry
        :task-node (game-task-node sewer-kg-met-introduction)
        :event-idx 0
        :continue "hiphog-start"
        :text-id (text-id progress-missions-sewer-kg-met)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node sewer-kg-met-introduction))
              (task-node-open? (game-task-node sewer-kg-met-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node sewer-kg-met-resolution)))
         )
      (static-taskable-menu-entry
        :task-node (game-task-node city-port-assault-introduction)
        :event-idx 0
        :continue "hiphog-start"
        :text-id (text-id progress-missions-city-port-assault)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node city-port-assault-introduction))
              (task-node-open? (game-task-node city-port-assault-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node city-port-assault-resolution)))
         )
      (static-taskable-menu-entry
        :task-node (game-task-node city-blow-barricade-introduction)
        :event-idx 0
        :continue #f
        :text-id (text-id progress-missions-city-blow-barricade)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node city-blow-barricade-introduction))
              (task-node-open? (game-task-node city-blow-barricade-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node city-blow-barricade-resolution)))
         )
      (static-taskable-menu-entry
        :task-node (game-task-node factory-sky-battle-introduction)
        :event-idx 0
        :continue #f
        :text-id (text-id progress-missions-factory-sky-battle)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node factory-sky-battle-introduction))
              (task-node-open? (game-task-node factory-sky-battle-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node factory-sky-battle-resolution)))
         )
    )
  )
  (define *tess-menu-list*
    (new 'static 'boxed-array :type taskable-menu-entry
      (static-taskable-menu-entry
        :task-node (game-task-node city-gun-course-1-introduction)
        :event-idx 0
        :continue "gungame-1-start"
        :text-id (text-id progress-missions-city-gun-course-1)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node city-gun-course-1-introduction))
              (task-node-open? (game-task-node city-gun-course-1-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node city-gun-course-1-resolution)))
         )
      (static-taskable-menu-entry
        :task-node (game-task-node city-gun-course-2-introduction)
        :event-idx 0
        :continue "gungame-2-start"
        :text-id (text-id progress-missions-city-gun-course-2)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node city-gun-course-2-introduction))
              (task-node-open? (game-task-node city-gun-course-2-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node city-gun-course-2-resolution)))
         )
    )
  )
  (define *sig-menu-list*
    (new 'static 'boxed-array :type taskable-menu-entry
      (static-taskable-menu-entry
        :task-node (game-task-node desert-artifact-race-2-introduction)
        :event-idx 0
        :continue #f
        :text-id (text-id progress-missions-desert-artifact-race-2)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node desert-artifact-race-2-introduction))
              (task-node-open? (game-task-node desert-artifact-race-2-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node desert-artifact-race-2-resolution)))
         )
      (static-taskable-menu-entry
        :task-node (game-task-node nest-hunt-introduction)
        :event-idx 0
        :continue "nstb-hunt"
        :text-id (text-id progress-missions-nest-hunt)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node nest-hunt-introduction))
              (task-node-open? (game-task-node nest-hunt-sig))
              (task-node-open? (game-task-node nest-hunt-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node nest-hunt-resolution)))
         )
    )
  )
  (define *onin-menu-list*
    (new 'static 'boxed-array :type taskable-menu-entry
      (static-taskable-menu-entry
        :task-node (game-task-node palace-ruins-patrol-introduction)
        :event-idx 0
        :continue "onintent-end"
        :text-id (text-id progress-missions-palace-ruins-patrol)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node palace-ruins-patrol-introduction))
              (task-node-open? (game-task-node palace-ruins-patrol-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node palace-ruins-patrol-resolution)))
         )
    )
  )
  (define *samos-menu-list*
    (new 'static 'boxed-array :type taskable-menu-entry
      (static-taskable-menu-entry
        :task-node (game-task-node sewer-met-hum-introduction)
        :event-idx 0
        :continue #f
        :text-id (text-id progress-missions-sewer-met-hum)
        :allow-when (lambda ()
          (or (task-node-open? (game-task-node sewer-met-hum-introduction))
              (task-node-open? (game-task-node sewer-met-hum-resolution))
              )
          )
        :forbid-when (lambda () (task-node-closed? (game-task-node sewer-met-hum-resolution)))
         )
    )
  )